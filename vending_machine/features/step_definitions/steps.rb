Given(/^item (.) costs \$(\d+\.\d+)$/) do |item_id, cost|
  pending # express the regexp above with the code you wish you had
end

When(/^(\d+) (\w+) are inserted into the coin slot$/) do |count, coins|
  pending # express the regexp above with the code you wish you had
end

And(/^the Get\-(.) button is depressed$/) do |item_id|
  pending # express the regexp above with the code you wish you had
end

Then(/^item (.) is dispensed$/) do |item_id|
  pending # express the regexp above with the code you wish you had
end